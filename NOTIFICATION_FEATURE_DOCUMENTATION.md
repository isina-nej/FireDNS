# مستندات قابلیت‌های جدید اپلیکیشن Fire DNS

## قابلیت‌های اضافه شده

### 1. منوی همبرگری
- اضافه کردن گزینه "بررسی بروزرسانی" به منوی همبرگری
- اضافه کردن گزینه "ارسال تیکت" به منوی همبرگری

### 2. صفحه بروزرسانی
- ایجاد صفحه نمایش پیام "شما از آخرین نسخه برنامه استفاده می‌کنید"
- اضافه کردن قابلیت حالت تاریک به صفحه بروزرسانی اجباری

### 3. صفحه ارسال تیکت
- ایجاد صفحه ارسال تیکت با پشتیبانی از حالت تاریک و روشن
- امکان ارسال تیکت با عنوان، متن و اولویت

### 4. سیستم اعلانات
- تبدیل صفحه اعلانات به پاپ‌آپ با پشتیبانی از حالت تاریک و روشن
- اتصال اعلانات به API و نمایش اعلان‌های دیده نشده
- اضافه کردن انیمیشن تکان خوردن به زنگوله برای اعلان‌های دیده نشده
- امکان علامت‌گذاری اعلان‌ها به عنوان خوانده شده
- امکان حذف اعلان‌ها

### 5. حالت تاریک
- اضافه کردن حالت تاریک به صفحات تنظیمات و درباره ما
- بهبود رابط کاربری در حالت تاریک

## جزئیات فنی

### 1. ساختار فایل‌ها
- `lib/widgets/notification_popup.dart`: ویجت پاپ‌آپ اعلانات
- `lib/widgets/notification_bell.dart`: ویجت زنگوله اعلانات با انیمیشن
- `lib/api/models/notification_model.dart`: مدل داده اعلانات
- `lib/api/services/notification_api_service.dart`: سرویس API برای اعلانات
- `lib/services/notification_service.dart`: سرویس مدیریت اعلانات
- `lib/screens/check_update_page.dart`: صفحه بررسی بروزرسانی
- `lib/screens/ticket_page.dart`: صفحه ارسال تیکت
- `lib/screens/force_update_page.dart`: صفحه بروزرسانی اجباری
- `lib/screens/settings_page.dart`: صفحه تنظیمات با پشتیبانی از حالت تاریک
- `lib/screens/about_page.dart`: صفحه درباره ما با پشتیبانی از حالت تاریک

### 2. ویژگی‌های پاپ‌آپ اعلانات
- نمایش اعلانات با رنگ‌بندی متناسب با نوع اعلان (اطلاعات، هشدار، خطا، موفقیت)
- امکان علامت‌گذاری تکی یا همه اعلان‌ها به عنوان خوانده شده
- امکان حذف اعلان‌ها
- پشتیبانی از لینک‌های عملیاتی در اعلان‌ها
- نمایش تاریخ و زمان اعلان
- پشتیبانی کامل از حالت تاریک و روشن

### 3. ویژگی‌های زنگوله اعلانات
- نمایش تعداد اعلان‌های خوانده نشده
- انیمیشن تکان خوردن زنگوله برای اعلان‌های دیده نشده
- باز کردن پاپ‌آپ اعلانات با کلیک روی زنگوله

### 4. سرویس اعلانات
- دریافت اعلانات از سرور
- مدیریت وضعیت خوانده شدن اعلان‌ها
- مدیریت حذف اعلان‌ها
- بروزرسانی خودکار اعلانات هر 5 دقیقه

### 5. حالت تاریک
- پشتیبانی از حالت تاریک در تمام صفحات جدید
- امکان تغییر حالت تاریک/روشن از صفحه تنظیمات
- ذخیره تنظیمات حالت تاریک/روشن در حافظه دستگاه

## نحوه استفاده

### 1. بررسی بروزرسانی
1. باز کردن منوی همبرگری
2. انتخاب گزینه "بررسی بروزرسانی"
3. مشاهده وضعیت بروزرسانی

### 2. ارسال تیکت
1. باز کردن منوی همبرگری
2. انتخاب گزینه "ارسال تیکت"
3. پر کردن فرم تیکت و ارسال

### 3. مشاهده اعلانات
1. کلیک روی آیکون زنگوله در نوار بالای برنامه
2. مشاهده لیست اعلانات در پاپ‌آپ
3. کلیک روی اعلان برای علامت‌گذاری به عنوان خوانده شده و باز کردن لینک (در صورت وجود)
4. کلیک روی آیکون حذف برای حذف اعلان
5. کلیک روی "خواندن همه" برای علامت‌گذاری همه اعلان‌ها به عنوان خوانده شده

### 4. تغییر حالت تاریک/روشن
1. باز کردن صفحه تنظیمات
2. انتخاب گزینه "تم برنامه"
3. تغییر بین حالت‌های تاریک، روشن یا سیستم